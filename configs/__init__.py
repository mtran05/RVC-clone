"""Device and chunk settings used by real-time and offline inference.

config.py picks the GPU, precision, and how long an offline file is split
into. config.json is leftover real-time GUI preferences and is not loaded
by the Python entry points.
"""
