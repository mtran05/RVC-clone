"""Synthesizer network used at inference time.

models.py builds the v1 and v2 voice models.
modules.py and attentions.py are the layers those models call.
transforms.py is the spline used by the normalizing flow.
commons.py holds the small tensor helpers those layers share.
"""
