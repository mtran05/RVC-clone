# Python 3.12 x64 / Windows + Linux / NVIDIA CUDA 12.8
#
# Torch and Torchaudio are installed separately so this file cannot replace them:
#   python -m pip install torch==2.7.1+cu128 torchaudio==2.7.1+cu128 --index-url https://download.pytorch.org/whl/cu128 --extra-index-url https://pypi.org/simple
#   python -m pip install -r requirements.txt

--index-url https://pypi.org/simple/

# Voice conversion inference only: real-time playback and offline file conversion.
faiss-cpu>=1.13.0,<2
librosa>=0.10.2,<0.11
numpy>=1.26.4,<2
praat-parselmouth>=0.4.5,<1
scipy>=1.13.1,<2
sounddevice>=0.5.0,<1
soundfile>=0.13.0,<1
torchfcpe>=0.0.4,<0.1
tqdm>=4.67.0,<5
transformers>=4.49.0,<4.50
